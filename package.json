{
  "name": "heroku-docker",
  "version": "1.0.0",
  "description": "Build and deploy a Docker image to Heroku",
  "main": "index.js",
  "scripts": {
    "build": "mkdir -p dist && showdown makehtml -i README.md -o dist/index.html"
  },
  "repository": "git@github.com:treadup/heroku-docker.git",
  "author": "Henrik Treadup <henrik.treadup@gmail.com>",
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "showdown": "^1.9.0"
  }
}
